# How to Install Ansible

Ansible can be installed on various operating systems using different methods. Here are the installation steps for some common platforms:

## On Ubuntu/Debian:
```bash
sudo apt update
sudo apt install ansible

## On CentOS/RHEL:
sudo yum install epel-release
sudo yum install ansible
